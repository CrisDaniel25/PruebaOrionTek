export interface IAdresses {
    addressId: number,
    country: string,
    city: string,
    street: string,
    building: string,
    zip: number,
    customerId: number,
    createdAt: Date,
    updatedAt: Date,
}